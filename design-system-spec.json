{
  "name": "dataleap-design-system",
  "description": "shadcn/ui skeleton with the DataLeap brand pinned: brand-green primary (#18A00D/#5AC750), six-color green-anchored categorical chart scale (chart-1..6), stock neutral oklch surfaces, Geist font. 60 components installed verbatim via `shadcn add --all`.",
  "framework": "react",
  "language": "typescript",
  "styling": "tailwind-v4-css-variables",
  "tokens": {
    "css": "src/index.css",
    "notes": "Semantic CSS variables in :root (light) and .dark (dark). Radius scale derives from the single --radius base. Values are oklch."
  },
  "components": {
    "directory": "src/components/ui",
    "notes": "One file per component, stock shadcn source. Import alias: @/components/ui/<name>."
  },
  "utils": "src/lib/utils.ts",
  "config": "components.json",
  "designDoc": "DESIGN.md"
}
